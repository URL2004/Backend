# v2.5.76 / 감지 v1.44 — 단위 기호·PDF 문장 소속·수치 공백

2026-09-26. 기준 운영 커밋 `bab2c4b` / v2.5.75. 사용자 원문, 결과, UID, 실행기는 저장소에 포함하지 않는다.

## 패치노트

- 각도·영미 단위·도함수의 ASCII prime을 인용 시작으로 오인하지 않는다. 실제 숫자 인용과 단위가 포함된 인용은 유지하며 문자와 위치를 바꾸지 않는다.
- 반복된 물리행과 어절 연결 증거가 있는 PDF형 입력에서 과거형 종결어미가 목록 표지로 고정되는 문제를 수정했다. 실제 가나다 목록은 유지한다.
- 완결된 본문 문단 안에서 종속 조사·어미와 주변 행의 근거가 모두 있는 탭만 정렬 공백으로 처리한다. 실제 표, 빈 셀, 코드, 참고문헌은 새 정규화 대상에서 제외한다. 공통 구조 분류와 입력 정규화가 같은 규칙을 사용한다.
- 고쳐 쓴 문장 바로 뒤에 유일한 원문 소유 문장이 재출현하는 인접 중복을 검토 대상으로 잡는다. 서로 다른 수치·부정·원문 주장과 인용은 구분하며 유사도만으로 삭제하지 않는다.
- 페이지 경계의 의존 서술부는 남고 앞부분만 재작성되는 신규 문장 손상을 경고한다. 신규 관측값은 코드·위치·개수에 한정한다.
- 소수 뒤 공백 유무에 따라 사실 추출 결과가 달라지던 결함을 수정했다. 동일 수치를 신규 사실로 오인하지 않으며 실제 값·부호 변화는 계속 검출한다.

모델·추론 강도·요금·전달·과금 정책은 변경하지 않았다. 신규 기술 차단이나 무차감 정책은 없다. 감지 v1.44는 공통 문장/인용 경계 변경을 캐시에서 구분하기 위한 버전이며 점수 산식·이력 보정은 변경하지 않았다. source preflight 30, Korean refinement 38, fragment integrity v2.

## 검증

- 최종 전체 자동 테스트: `node --test --test-concurrency=2 test/*.test.js` — **2,252/2,252 통과**, 실패·스킵 0.
- production import graph 통과, 위반 0. 모델 실패/재시도 및 개인정보 없는 경고 전달 회귀 포함.
- 기본 병렬 실행에서는 성능 1개가 환경 부하로 기준 초과했다. 임계값을 완화하지 않고 동일 검사 단독 및 동시성 2 전체 실행으로 재검증했다.
- 기존 개발 자료 원문 60건 + 해당 원문 1건 정규화 재생: 비공백 문자, 인용·표·참고문헌 보존 및 멱등성 검증. 해당 원문 본문 탭 2행은 별도로 직접 확인하여 실제 표 24행과 구분했다. 물리행 경계 91곳 정리. 이 결과는 새로운 생성/블라인드 정확도가 아니다.
- 같은 61건의 저장 결과 수치 감사 비교에서 기존 novelty 판정 변화 없음. 실제 신규 재생성의 수치 공백 오탐 3건은 저장 결과 재생에서 해소. 합성 테스트에서 값·부호 변경 탐지는 유지.

### 실제 전체 문서 재생성

동일 입력 5,290자, 운영 formal 모드/모델 설정 사용, 사용자 크레딧 차감·Firestore 쓰기 없음. 각 실행 중 코드가 바뀌지 않은 것을 해시로 확인했다.

| 실행 | 판단 | 시간 | HTTP 호출 | 추정 API USD |
|---|---|---:|---:|---:|
| 1 | 기존 중복·의존 서술부 문제는 해소됐으나 종결어미/본문 탭 분리 손상 확인. 배포 탈락 | 333.6초 | 26 | 0.291281840 |
| 2 | 추가 경계 수정 후 최종 의미 pass, 위반 0, 문장 조각 0. 수치 공백 경고 오탐을 발견해 추가 수정 | 312.6초 | 19 | 0.184747695 |
| 3 | 최종 코드 전문 재생성. 의미 pass/exact digest, 위반 0, 문장 조각 0, 해당 인접 중복 미재현, 수치·인용 보존 | 211.2초 | 14 | 0.114850265 |

총 59 HTTP 호출 **$0.590879800**, 보수 환산 2,000원/USD에서 **약 1,182원**. 실패·재시도·보조 호출 포함, 미확정 사용량 0. 승인 3,000원(호출 2,400원 + 예비 600원) 이내. 실제 청구서 확정액이나 운영 p95가 아니다.

최종본 전체 문장과 표를 원문과 직접 대조했다. 설명 관계·수치·자료의 한계·개인 판단이 유지되고 문제의 중복 및 문장 파손은 재현되지 않았다.

## 남은 한계

- 최종 3차 결과의 `needs_review`는 기존 `paragraph_structure_changed`, `semantic_relation_shift` 경고다. 의미 심사 통과만으로 휴리스틱 경고를 삭제하거나 모두 정상이라고 표시하지 않았다.
- 원본 PDF가 없어 이미 손상된 표의 열 배치를 복원하지 않았다. 일부 원문 추출 어절 공백·여분 빈 줄은 남는다. 모든 PDF 서식 및 한국어 띄어쓰기의 완전 복원을 주장하지 않는다.
- 새 물리행 정규화의 참고문헌 제외를 검증했다. 기존 후속 preflight의 일부 참고문헌 행 공백 처리까지 전부 변경한 것은 아니다.
- 소표본 3회에서 해당 오류가 재현되지 않았다는 결과이며 모든 미래 글의 무오류 보장이 아니다.

## 배포 절차

현재 운영 계보 확인 → 깨끗한 테스트 완료 기능 브랜치 커밋 → 환경/healthz/activeJobs 0 재확인 → 기능 브랜치 및 production 비강제 푸시 → Render live 커밋·엔진 버전 확인. 환경변수와 프런트 화면은 변경하지 않는다. 이상 시 직전 운영 커밋을 재배포한다.
