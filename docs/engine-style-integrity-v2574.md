# v2.5.74 / 감지 v1.43 검증 기록

작성일: 2026-09-26. 기준 운영 커밋: `6512de3` (v2.5.73 / v1.42).
초기 보관 브랜치: `archive/wip-style-integrity-v2-5-74-20260926`. 추가 검증 작업: `fix/style-integrity-validation-20260926`, 후속 보관: `archive/wip-style-integrity-followup-20260926`. 아래 기록은 시간 역순이며 **최신 최종 검증 기록이 이전 배포 보류 판단을 대체**한다.

## 최종 추가 3,000원 검증 — 통합 배포 게이트 통과

지정한 신규 의미·구조 오류의 최종본 대조를 완료했다. 감지·화면만 먼저 배포하지 않고 휴머나이징과 함께 배포한다. 이 판단은 모든 출력의 무오류 보장이나 모든 경고의 해소를 뜻하지 않는다. 실제 운영 반영 여부와 커밋은 별도 배포 기록에서 확인한다.

### 마지막 재현에서 수정한 원인

- 반복되는 같은 수식 중 뒤쪽 수식을 먼저 잠금 경계로 선택하던 탐색을 다음 고유 잠금 블록 앞까지 제한했다. 중간 수식의 누락·순서 왜곡과 중복 삽입을 막으며 불확실한 위치를 새로 만들지 않는다.
- 원문 수식들이 모두 정확한 순서로 남고, 원문에 있던 단순 식별자에만 새 인라인 수식 포장이 붙은 경우 그 포장만 제거한다. 실제 수식 변경·누락·순서 변경은 계속 실패로 남는다.
- `2.4에서는` 같은 절 참조는 제목이 아닌 본문으로 분류한다. 실제 번호 제목은 유지한다. source preflight 버전은 28이다.
- 의존 명사와 무관한 단어까지 나누던 공백 수리를 문맥으로 제한했다. 이미 정상인 부사에 새 띄어쓰기 오류가 생기지 않는다.
- 빈 문단 경계 너머의 숫자와 다음 문단 첫 글자를 단위 표현 하나로 읽지 않는다. PDF의 한 줄 개행과 실제 수량 변경 검사는 유지한다.

### 실제 모델·최종본 검증

| 사례 | 실행과 최종 확인 | 비용·시간 |
|---|---|---|
| H0051 | 27,795자 장문 전체 신규 생성. 4구간 의미 pass·구조 pass. 계수/함수 대응, 우함수·기함수 논증, 조건별 설명을 전문 대조 | 약 495초, $0.503829115 |
| H0051 후처리 확인 | 같은 입력·모델·스키마의 실제 저장 응답 34개만 재사용하고 달라진 심사 입력은 새 호출. 최종 의미 exact pass·구조 pass. 전체 결과 차이는 확인된 공백 오류 한 글자뿐 | 약 314초, 신규 비용 $0.023166195. 새 전체 재생성으로 세지 않음 |
| H0055 최종 | 14,401자 수식 장문 전체 신규 생성. 2구간 의미 pass·구조 pass, 15개 잠금 블록과 4개 제목 보존. 수식 순서·본문 참조·문단 연결을 전문 대조 | 약 347초, $0.219945690 |
| H0055 앞선 실패 | 의미 pass와 별개로 수식 경계 2곳 손실. 불합격으로 남기고 원인 수정 후 재실행 | 약 303초, $0.115381260 |

H0055 최종 실행에서는 일반 변환과 특정 함수의 변환을 혼동한 범위 누락을 상위 심사가 검출하고 국소 복구 후 재검증했다. 마지막 숫자/단위 오탐 수정은 저장 최종본의 결정론적 재생 및 회귀로 검증했으며 이후 전체 모델 재생성을 했다고 표현하지 않는다.

남은 경고도 전문과 대조했다. H0051의 집단 화자는 남아 있으며 단수 화자 경고는 보조용언을 대명사로 읽은 오탐이다. 인접 반복 후보는 계수의 성질과 실제 공식을 별도로 설명하는 두 문장으로 원문 대응도 각각 존재한다. 이를 경고 제거 목적으로 삭제하지 않았다. 의미 관계 휴리스틱 후보, 일부 기존 반복·장르 표현 및 문단 길이/정형 표현 목표 경고는 남아 있다. 경고를 숨기거나 의미 심사 pass만으로 모든 품질 점검이 통과했다고 표시하지 않는다.

### 최종 검증·예산·제한

- Backend **2,160/2,160 통과**, production import 위반 **0**. Frontend **348 통과·1 기존 조건부 스킵·실패 0**. 저장소 밖 빌드와 화면 검증은 최종 패치노트 갱신 후 다시 확인한다.
- 기존 60쌍·감지 53건의 로컬 재생 완료. 신규 전체 생성, 후처리 재생, 심사 전용 호출을 구분했으며 113건 전량을 새 모델로 생성했다고 주장하지 않는다.
- 최종 승인 3,000원(호출 2,400원·예비 600원): 실제 HTTP 시도 **116회**, 사용량 확인 **113회**, 미확정 실패 **3회**. 확인 **$0.817221635** + 미확정 예약 **$0.045100625** = 상한 **$0.862322260**, 관리 환율 2,000원/USD에서 **1,725원(올림)**. 활성 예약 없음. 앞선 원장과 합쳐 잔액을 재사용하지 않았으며 저장 응답 재생에 새 비용을 이중 집계하지 않았다.
- 감지의 강/약 문체 대조·반복 변동·UI 범위는 아래 기록과 같다. 일괄 점수 가산, 모델·요금·전달·과금 정책 변경은 없다. 운영 설정은 이력 보정 하한 10과 독립 통계 점수 영향 OFF 두 항목만 개별 적용한다.
- 소표본을 전체 감지 정확도·운영 p95로 발표하지 않는다. 원본 없는 PDF 배치 복원, 모든 브라우저의 네이티브 확대, 운영 인증/결제 E2E 및 배포 후 1·6·24·72시간 경과 점검을 완료했다고 주장하지 않는다.

## 2차 추가 5,000원 검증 — 당시 상태(최종 검증 전)

2026-09-26 재확인 운영 커밋은 Backend `6512de3`, Frontend `a05f078`이다. 작업 브랜치는 각각 `fix/style-integrity-release-gates-20260926`, `fix/style-score-release-gates-20260926`이며 운영 코드·설정은 변경하지 않았다. **사용자 선택: 감지·화면만 먼저 배포하지 않고 휴머나이징까지 통과한 뒤 함께 배포한다.**

### 실제로 찾고 수정한 원인

| 원인 | 수정과 확인 |
|---|---|
| 전문용어·숫자·일인칭이 있다는 이유로 추상·반복 문체를 과도하게 상쇄 | 감지 프롬프트 v8: 구체적인 의사결정·제약·결과가 실제로 반복을 끊는 구간만 반대 근거로 평가. 전체 본문 근거를 먼저 출력하고 점수를 뒤에 산출 |
| 위치가 확인된 반복 신호가 넓게 있는데 낮은 점수·높은 신뢰도로 재심사를 피함 | `detectEvidenceReview.js`: 충분한 독립 문장과 검증된 위치가 있을 때 기존 상위 모델 재심사. 점수를 가산하거나 높은 쪽을 선택하지 않으며 더 낮은 재심사 결과도 채택 |
| 장문은 근거 예시 상한 8개 때문에 분모가 커질수록 재심사 불가 | strong/pervasive 관찰의 실제 위치가 문서 여러 구간에 분산된 경우만 별도 검토. 표본을 전체 근거 커버리지로 부풀리지 않음 |
| 문장 위치가 밀리면 비슷한 다른 조건 문장에 원문을 덮어씀 | `sourceSentenceRestore.js`: 지역 위치만으로 소속을 확정하지 않고 다른 실제 대응 문장과 경쟁 소유를 확인 |
| 긴 원문을 5문장으로 나눈 결과 중 1~3문장에만 원문 전체를 복사 | 분리된 나머지 절의 소속이 불확실하거나 복원 상한을 넘으면 부분 덮어쓰기를 하지 않음. 문단 합침·유사 문장 자동 삭제 없음. 실제 저장 응답 재생과 합성 회귀로 재현 해소 |
| 서식 정리가 잠금 토큰을 본문에 붙여 작은 의미 수정을 거절 | 서식 처리 후 잠금 경계를 다시 확인하고, 복원된 후보에는 원문 기준 문체 프로필을 사용 |
| 심사 모델이 줄바꿈을 공백으로 인용하면 정확한 복구 위치를 못 찾음 | `evidenceSpan.js`: 공백 묶음만 정규화하고 실제 UTF-16 위치로 되돌림. 단어·문장부호 변경, 중복 근거, 빠진 중간 내용은 허용하지 않음 |
| 하위 심사 통과가 잘못된 계수 대응을 놓치거나 사소한 수정에 유일한 수리 기회를 소진 | 서버가 찾은 신규 변수·수치·주체 대응 후보는 기존 설정의 상위 심사를 처음부터 사용. 수리·재심사 횟수 상한 유지, 휴리스틱 자체는 오류 확정이나 통과 판정이 아님 |

### 검증 결과와 한계

- 백엔드 **2,151/2,151** 통과, production import 위반 **0**. 프런트 **348 통과·1 조건부 스킵·실패 0**. 저장소 밖 운영 형식 빌드와 데스크톱/360/390px/확대 대응 레이아웃 점검 완료. 확대 대응은 640 CSS px 재배치이며 모든 브라우저의 네이티브 확대 검증은 아니다.
- 기존 60쌍·감지 53건 로컬 재생 완료. 제목 보존/수식 오탐과 실제 소제목 누락을 구별한다. 113건 조치표는 개발 자료이며 새 블라인드 성과로 재분류하지 않는다.
- H0059 **최신 전체 엔진 실호출**: 약 **141초**, **$0.102147**, 최종 의미 pass·구조 pass. 전문 직접 대조에서 인물의 행위 관계, 원문 추측 강도, 인용, 소제목을 확인했고 과거 부분 원문 복사로 생긴 중복이 재현되지 않았다. 후보 수준 `semantic_relation_shift` 경고 1개는 남아 있어 전체 경고 0이라고 하지 않는다.
- H0051 최신 **저장 결과의 전체 문맥 감사·국소 수리**: 약 **48초**, **$0.131173**, 4구간 의미 pass·무결성 pass. 잘못된 계수 대응과 단정 강도 변화가 수정됐음을 직접 대조했다. 감사 중 원문·후보의 기본 문맥은 유지했지만 이 단계 실행은 신규 전체 재생성과 다르다.
- 그 직전의 하위 심사 단독 pass는 실제 계수 오류를 놓쳤다. 이를 합격 실적으로 세지 않았고 상위 모델 우선 심사로 보완했다. 심사 응답의 pass만으로 수동 문맥 대조를 대체하지 않는다.
- H0051 최신 전체 생성 재실행과 H0055 수식 구조의 최신 전체 재실행은 **미완료**다. 수정 전 전체 실행 4회와 후속 감사 3회를 모두 최종 성공 표본으로 합산하지 않는다.
- 감지 최신 실제 입력 6건 × 3회 캐시 우회 반복에서 점수 범위는 각각 **24~34 / 31~33 / 54 / 27~29 / 30~34 / 17~30**이었다. 반복 변동이 남아 있으며 완전한 재현성을 주장하지 않는다. 일반 응답 캐시는 별도 회귀 테스트 대상이다.
- 이번 추가 승인 원장: HTTP 실호출/재시도 **229회**, 확인 비용 **$1.84619797**, 미확정 예약 **$0.022646125**, 합계 상한 **$1.868844095**. 관리 환율 2,000원/USD로 **3,738원(올림)**. 실패·버린 후보·보조 심사·캐시 쓰기를 포함한다. 앞선 두 승인 원장과 구분하며 청구서 확정 원화 금액은 아니다.
- 호출 가능액 4,000원과 예비 1,000원을 구분한다. 확인 비용 기준 호출 잔액은 약 308원으로 장문 전체 실행과 최종 심사의 사전 예약을 충족하지 못한다. 추가 유료 호출 및 운영 배포는 하지 않았다.

### 저점수 검증: 새 문체 대조군

아래 6개 합성 입력은 이번 개발 대조에 쓰지 않은 주제이며, 강/약 기준을 결과 확인 전에 작성했다. 전부 AI로 구성한 문체 통제 자료다. 사람/AI 작성 판별 정확도나 운영의 적정 평균을 증명하지 않는다.

| 입력 유형 | 현행 | 수정판 |
|---|---:|---:|
| 학교 운영 보고·강한 추상 반복 | 70 | 86 |
| 같은 주제·측정 조건과 반례 전개 | 5 | 4 |
| 고객 응대 안내·강한 상투 전개 | 64 | 72 |
| 같은 주제·구체적인 처리 절차 | 8 | 8 |
| 제품 후기·강한 일반적 칭찬 반복 | 66 | 68 |
| 같은 주제·사용 조건과 한계 전개 | 8 | 4 |

최근 실제 53건의 낮은 분포 전체가 정상이라고 확정한 것은 아니다. 다만 이력 보정의 전체 평균 영향은 약 −0.5점이었고, 기본 모델의 반대 근거 판단과 점수/근거 모순을 먼저 고쳤다. 일괄 가산·정규분포 맞춤·외부 검사 통과 보장은 추가하지 않았다. 보정 하한 10 및 독립 통계 OFF는 여전히 **배포 대기 설정**이며 운영에 몰래 선적용하지 않는다.

### UI 범위와 다음 합격 조건

impeccable의 명료화 원칙에 따라 사용자 화면은 ‘AI식 문체 점수’ 명칭을 유지하고 원치 않은 보정/짧은 글/근거 제한 안내를 노출하지 않는다. 실제 미측정 데이터를 만들어 채우지는 않는다. 화면 검증 20상태는 전후 합성 페이로드 비교이며 운영 로그인·결제 E2E가 아니다. 관리자 패치노트는 완료/미완료를 분리했다. 기존 CTA 금액 경고색 대비 및 디자인 사이드카 갱신 경고는 이번 변경 외 범위로 별도 기록했으며 전체 사이트 접근성 통과를 주장하지 않는다.

남은 장문 전체 실호출과 최종본 직접 대조를 통과한 다음에만 통합 배포한다. 배포 전 깨끗한 Git 상태, 현재 운영 계보, `/healthz`, `activeJobs:0`, 백엔드→프런트 순서 및 두 선택 설정 변경을 재확인한다. 배포 후 1·6·24·72시간 점검은 아직 시작하거나 자동 등록한 상태가 아니다.

## 추가 5,000원 검증 — 2026-09-26 최종 갱신

아래의 기존 10,000원 단계 기록은 이력으로 유지한다. 현재 상태와 수치는 이 절이 우선한다.

- 전체 테스트: 백엔드 **2,134/2,134**, 프런트 **348 통과·1 조건부 스킵·실패 0**. production import **238파일·724연결** 통과, 저장소 밖 운영 형식 빌드 성공.
- 추가 실호출 230회. 확인 비용 **$1.955447145**, 사용량 미확정 2회 예약 **$0.315095125**, 합계 상한 **$2.27054227**. 보수적 환율 2,000원/USD에서 **4,542원(올림)**이며 카드 청구액은 아니다. 실패·버린 후보·재심사·캐시 쓰기 비용을 포함한다. 앞선 승인액과 원장을 분리했다.
- 5,000원 중 호출 4,000원·실패/환율 여유 1,000원으로 관리했다. 사용량이 확인된 잘린 판정 응답 2회의 $0.1777238과 미확정 예약은 실패 여유에 포함했으며, 비용을 0원으로 지우지 않았다. 현재 전체 상한 잔액은 약 458원이다. 장문 생성·보조 호출·최종 재검증 전체를 보장할 수 없어 추가 호출을 종료했다.
- 휴머나이징 6문서 전체 실행 후 국소 재심사 4회, 후속 전체 실행 4회(2회 미완료), 장문 국소 감사 1회(미완료), 저장된 심사 쌍의 상위 모델 재검증 1회를 수행했다. 별도 합성 문체 대조 6입력은 현행/수정판 각각 실행했다. 반복 실행을 독립 문서 수로 세지 않는다.
- H0046 전체 실행에서 의미 pass. H0051·H0052·H0060 국소 재심사는 pass였지만, 최신 전체 파이프라인 통과로 환산하지 않는다. H0055 전체 실행의 의미 pass와 별개로 수식 마스킹 표현 불일치에 의한 구조 경고를 발견했다.
- H0059에서는 1차 심사의 행위 주체 오판이 잘못된 복구를 유도하는 사례를 발견했다. 상위 심사를 수정 전에 수행하도록 변경했다. 마지막 상위 심사 단독 실호출은 38.6초에 잘림 없이 반환됐지만, 관계 오류/일부 불확실 판정이 남았다. **응답 완성은 내용 통과가 아니며, 이 사례는 미해결이다.**
- H0051에서는 원문 대비 길이 밴드 때문에 작은 사실 복구까지 거절되고 최종 재검증으로 밀리는 원인을 확인했다. 이미 밴드를 벗어난 후보라도 길이 위반을 악화시키지 않는 5% 이하 국소 수정이고, 새 의미 검증이 완료돼 pass인 경우에만 수용하도록 회귀 테스트를 추가했다. 최신 조합의 전체 유료 재실행은 미완료다.
- 구조 계산 로컬 합성 벤치마크: 20문단·120문장, 동시 3작업, 10회에서 배치 p95 **78.9ms**, 이벤트 루프 p95 **26.9ms**, 최대 **28.0ms**. 순서·내용·대기 작업 취소 통과. 요청 안에서 문장/인용 분석을 재사용한다. 이는 운영 p95가 아니다.
- 추가 합성 문체 대조군의 현행→수정판: 강한 보고서 91→94 / 약한 보고서 8→4, 강한 지원서 79→87 / 약한 지원서 8→8, 강한 설명문 88→92 / 약한 설명문 14→8. 모두 AI로 구성한 문체 통제 자료이며 사람/AI 작성 판별 정확도 자료가 아니다. 점수가 항상 낮게 고정되지는 않지만 소수 대조군으로 저점수 분포 전체가 정상이라고 확정할 수도 없다. 일괄 가산은 하지 않았다.
- 사용자 최종 요청을 우선해 화면에는 **AI식 문체 점수** 명칭만 정리하고 이력 보정·짧은 글·분석 근거 제한 안내는 제거했다. 핵심 문장·원인 분석·개선 포인트와 확인된 값은 유지하며 미측정 데이터를 만들지 않는다. 서버 진단 및 보정 계산은 유지한다.
- Vercel CLI 인증은 재확인 시 유효했다. **현재 배포 보류 사유는 인증이 아니라 핵심 품질 검증 미완료다.** 운영 브랜치·서비스·보정 하한·독립 통계 설정은 변경하지 않았다. 후속 보관 브랜치는 운영 배포본이 아니다.

### 후속 코드 원인과 검증 범위

| 원인 | 구현 | 미완료 범위 |
|---|---|---|
| 복구 모델이 전체 본문을 다시 쓰며 정상 문장까지 변경 | `relationPatch.js`: 서버 소유 ID, 정확하고 유일한 구간, 중복/누락 ID 거부, 최대 4창·각 1,800자, 새 의미 심사 필수 | 최신 전체 장문 실호출 |
| 1차 주체·변수 오판이 상위 심사 전에 원문 관계를 바꿈 | `judge.js`: 설정된 상위 심사를 미수정 후보에 먼저 적용 | 대표 사례는 상위 판정 반환 확인만 완료; 최종 복구는 미완료 |
| 긴 상위 심사가 6,000토큰에서 잘려 다시 과금 | 판정 출력 10,000토큰 사전 예약, 판정 전용 잘림/시간 초과의 무조건 재시작 제거 | 장문별 판정 출력 충분성 전수 검증 아님 |
| 출력은 수식 복원 뒤, 원문 청크는 마스크 상태로 구조 대조 | `literalSpans.js`: 알려진 수식/코드의 감사용 투영, 원본 청크 ID·offset·마스크 불변 | 로컬·전체 파이프라인 모의 회귀 완료, 실제 장문 재생 미완료 |
| 이미 짧은 문서의 작은 사실 수정도 절대 길이 밴드가 거절 | `index.js`: 새 의미 pass·국소 변경·위반 비악화 조건 결합 | 마지막 유료 장문 감사는 수정 전 판정 출력 한도로 미완료 |
| 안전하게 원문 구절을 되살려도 기존 문체 경고가 복구 거절 | 명확한 원문 복원에 한해 신규 오류 0·기존 종결 경고만 허용, 경고 유지·재검증 필수 | 국소 확인과 전체 검증을 구분 |
| 반복 분석이 문단 정렬의 이벤트 루프를 지연 | `textAnalysisCache.js`: 요청별 256항목/100만 문자 상한, 반환 복사, 요청 간 비공유 | 로컬 동시 작업 목표 통과, 운영 부하 측정 아님 |
| 공백을 전부 지운 이력 해시가 다른 어절 의미도 일치시킴 | `detectCalibration.js`: 검색 해시는 유지하고 실제 일치에서 단어 경계를 별도 대조 | 다른 사용자·서명·변형·0~100 회귀 통과; 운영 신규 E2E 미실행 |

남은 합격 조건은 H0051 장문과 H0059 주체 관계의 **최신 코드 전체 실행 및 최종 의미·구조 검증**이다. 국소 통과 또는 단독 심사만으로 운영 반영하지 않는다. API 추가 비용이나 별도 분리 배포가 필요하면 사용자와 범위를 다시 합의한다.

## 범위와 정책

60쌍의 휴머나이징과 53건 감지 자료를 개발·회귀 자료로 사용했다. 제공된 종합 PDF, 개별 평가 PDF, 전문 대조 HTML을 읽고 원문·결과와 코드에서 확인한 사실을 분리했다. 보고서의 주관적 점수를 정확도 정답으로 채택하지 않았다. 동일 문서의 재편집 계보는 독립 표본이 아니다.

모델·추론 설정·요금·전달·과금 정책은 변경하지 않는다. 품질 경고가 있다는 이유로 새 차단이나 무차감을 도입하지 않는다. 원본 PDF가 없는 표 배치와 빠진 내용, 출처가 확인되지 않은 외부 사실은 추측해서 채우지 않는다.

개인 원문·결과·식별자·실호출 응답·API 러너는 저장소 밖 비공개 보고서 디렉터리에만 있다. 저장소의 신규 테스트는 비식별 합성 문장이다.

## 원인과 변경 지점

| 원인 | 수정 | 코드 |
|---|---|---|
| 단어가 남아도 주체·조건·변수 귀속이 바뀜 | 대응하는 원문/결과 구간, 관계 종류, 신규/기존/불확실 구분. 유일한 실제 구간만 복구 대상으로 인정 | `judge.js`, `relationAudit.js` |
| 다문장 지시 관계가 1:1 복구 제한에 막힘 | 정확한 쌍 근거, 완결 문장 경계, 보호문자·구조·순서·분량 제한 안에서 최대 3문장 창 복구 후보를 만들고 재심사 | `confirmedRelationRestore.js`, `finalQualityV2.js` |
| 의미 없는 강조 삭제를 내용 누락으로 심사 | 기능·조건은 보존한 수식어 축약과 실제 수량·인과·가능성 변경을 구분 | `judge.js` |
| 가까운 문장으로 분리되어 남은 내용을 누락으로 오판 | 선택한 결과 구간 밖 인접 문장에 상당한 원문 내용이 대응하면 복구를 유보. 실패 판정을 정상으로 바꾸는 휴리스틱이 아님 | `relationAudit.js`, `judge.js`, `confirmedRelationRestore.js` |
| 제목 공백 및 수식이 구조 개수에 영향을 줌 | 입력/청크/감사가 공유하는 역할 분류를 정리. 수식은 보호 코드, 통계 행은 별도 역할 | `layoutStructure.js` |
| 도입구 고립 및 연속 경험 문장 과분리 | 확실한 미완결 도입구만 연결. 명확한 경험→준비→계획 단계가 있는 연속 산문만 제한적으로 묶음 | `layoutRelations.js`, `structureChunk.js`, `proseParagraphs.js`, `voiceProfile.js` |
| 공백 축약값만 같으면 관계가 달라도 검증 재사용 | 구조 역할·위치·행·조건 관계의 비텍스트 해시가 같아야 공백 변경의 검증을 재사용 | `semanticProvenance.js`, `layoutRelations.js` |
| 최종 재검증의 새 오류가 복구 단계 뒤에서 발견됨 | 같은 120초 창 안에서 복구 후 재검증할 여유가 있을 때만 1회 확정 구간 복구. 확인 실패 시 이전 실패 판정 유지 | `index.js` |
| 이전 pass 후보가 나중에 확인한 오류도 포함할 수 있음 | 확인된 오류 구간을 포함하는 옛 후보를 최종 폴백에서 제외 | `candidateLedger.js` |
| 장르가 인사말·인용·단일 단서에 흔들림 | 질적 연구, 토론 입론, 개인 편지, 짧은 지원서의 목적 단서 결합 | `documentProfile.js` |
| 분석 문장·표시 문장·분석량 단위 불일치 | 공통 구문 분석에 기초한 sample unit과 원문 offset 전달. 프런트 재추정 제거 | `detectInputDocument.js`, `detectGrounding.js`, `detectReportLocations.js` |
| 근거 요구 0에서 coverage=1을 충분한 증거처럼 설명 | 해당 점수에서 근거 요구 없음과 실제 근거 충분성을 별도로 표시 | `detectSignalPolicy.js`, `detectInterpretation.js` |
| 독립 통계가 불연속적으로 49점까지 승격 | 독립 경로의 숫자 영향 OFF, 닫힌 스키마의 참고 정보 유지. 근거 결합 통계 경로는 유지 | `detectStatisticalAssist.js` |
| 이력 일치/무변화와 불일치 구분 불가 | 현재 점수와 일치하는 보정 전/후/조정값을 API·이력·관리자에 선택 필드로 추가 | `detectScorePresentation.js`, `historyService.js`, `detectreport.js`, `payment.js` |

## 감지 점수 해석

53건의 선택 모델 평균은 약 25.0, 통계 후 27.0, 표시 26.5점이었다. 이력 일치 4건 중 기존 하한으로 점수가 그대로인 1건이 있다. 이 표본의 낮은 평균을 이력 보정이나 근거 상한 감산 탓으로 단정할 수 없다. 작성 경위는 전건 미확인이다.

승인된 설정 변경은 이력 하한 20→10뿐이다. 운영의 감산 비율 40%, 기본 최대 감산 30점, 검증된 원문 점수 상한 및 동일 사용자·서명·보수적 일치 조건은 유지한다. 25→15, 27→16, 34→20이며 원점수 0~9를 올리지 않는다. 0~100 전체 단조성·원점수 비상승을 검사한다.

유사 이력 일치의 어휘 변경 허용은 보수적으로 좁혔다. 긴 글의 3% 이내 수정도 주체·긍정/부정·조건을 뒤집을 수 있어, 새 서버 결과 서명 없이 바뀐 핵심 내용을 검증할 수 없다. 정규화 정확 일치와 마침표 등 동등한 조판 차이는 허용하지만 단어가 바뀐 글은 자동 보정하지 않는다. 이는 기존 유사 일치 적용률이 낮아질 수 있는 명시적인 트레이드오프다.

캐시는 계속 보정 전 결과만 저장하고 새 요청에 현재 보정 설정을 적용한다. 과거 저장 점수·버전은 소급 갱신하지 않는다. 새 결과 명칭은 “AI식 문체 점수 · N/100”; AI 작성 확률이나 외부 검사 통과 확률이 아니다. 보정으로 인한 하락을 순수 문체 개선으로 표현하지 않는다.

독립 통계의 새 수치 매핑은 도입하지 않는다. 새 독립 검증 자료가 부족하므로 숫자 기여를 OFF로 유지한다. 단, 실제 문장 근거와 결합된 기존 통계 승격은 별도 출처를 표시하며 유지한다.

## 실호출 예산과 해석

총 승인 10,000원, 호출 가능액 8,000원, 환율·실패 여유 2,000원. 보수적 관리 환율 2,000원/USD로 $4까지 호출 전 예약한다. 최대 출력과 입력 상한, 캐시 쓰기 단가까지 예약하고 응답 토큰으로 정산한다. 사용량이 불명인 실패는 0원으로 놓지 않고 예약 전액을 유지한다. 이는 청구서의 확정 원화 금액과 다르다.

기본 6·고급 4·다듬기 2의 동일 입력 비교, 대표 감지 6개 입력의 3회 반복 비교, 별도 작성 경위 대조군을 구분한다. 초기 후보에서 재현한 잔여 오류도 숨기지 않는다. 수정 후 국소 단계 재심사와 전체 엔진 재실행은 서로 다른 검증 범위로 기록한다. 소표본으로 전체 정확도나 운영 p95를 주장하지 않는다.

## 배포 조건과 롤백

### 이번 검증 결과와 남은 제한

- 백엔드 전체 자동 테스트 2,114/2,114 통과, 프런트 347 통과·1 조건부 스킵·실패 0. 운영 import 검사 236파일·717연결 통과, 저장소 밖 빌드 통과. 합성 UI의 4가지 화면에서 수평 넘침·JS 오류 없음.
- 전체 엔진 실호출: 현행 12건 + 수정판 12건. 첫 수정판의 최종 의미 판정은 pass 6건 / fail 6건이었다. 더 엄격한 심사가 새로 잡은 오류와 실제 생성 오류가 섞여 있으므로 실패율만으로 품질 증감을 단정하지 않는다.
- 추가 국소 복구 재심사 7건 중 3건은 pass, 4건은 미완료였다. 미완료에는 시간 초과와 다음 모델 호출의 비용 예약 거절이 포함된다. 국소 3건 통과는 전체 엔진 최신본 재실행 통과와 다르다.
- 장문에는 실제 주체 관계 오류와 보존된 인접 문장을 누락으로 본 심사 오탐이 함께 있었다. 해당 내용은 같은 심사 구간 안에 있었으므로 이 사례를 정렬 경계 버그로 확정하지 않는다.
- 역할이 생략된 네 문장 창, 일부 비교·가능성 강도의 미세한 차이, 기존 제목 접합·문단 호흡은 남은 검토 대상이다. 안전 한계를 풀어 원문 전체를 덮거나 위치를 추측하지 않았다.
- 감지는 동일 6건 × 3회 × 2버전 = 36회, 별도 대조군 3건 × 2버전 = 6회를 실행했다. 직접 엔진 결과는 통계 이후·이력 보정 전 점수이며 운영 화면의 최종 표시 점수가 아니다.
- 실제 호출/재시도 360회. 사용량 확인분 $2.339467, 사용량 미확정 8회 예약 상한 $1.567241, 합계 상한 $3.906709 (관리 환율 기준 약 7,814원). 미확정 비용을 0원으로 처리하지 않았으며 추가 유료 호출을 중단했다.
- 반복·추상 전개를 강하게 넣은 별도 AI 글은 두 버전 모두 91점, 구체 장면 중심의 별도 AI 글은 4→3점, AI 이전 사람 글 발췌는 4→6점이었다. 작성 경위와 문체 점수는 같지 않다. 새 정확도·오탐률의 통계적 검증군으로는 부족하다.
- 최신 최종 복구 연결, 인접 내용 중복 방지, 조건별 문단 보완 및 유사 일치 강화는 자동 회귀를 추가했으나 그 전체 조합의 실호출 검증을 완료하지 못했다. 운영 코드는 v2.5.73 / 감지 v1.42와 기존 설정을 유지한다.
- 프런트 합성 결과 화면에서 데스크톱, 360/390px, 1280px의 200% 확대에 해당하는 640 CSS px·2배 픽셀 밀도 재배치를 점검했다. 초기 CSS zoom 방식은 미디어 쿼리 재배치를 재현하지 못해 폐기하고 전후 캡처를 다시 만들었다. 실제 운영 인증·신규 작업·결제 경로 테스트나 모든 브라우저의 네이티브 확대 검증은 아니다. 저장된 Vercel 인증도 만료되어 재인증이 필요하다.

핵심 검증을 마치기 전에는 이 코드·설정을 운영에 배포하지 않는다. 1·6·24·72시간 확인 항목은 준비했지만 배포하지 않았으므로 사후 점검 시작 또는 자동 등록을 완료한 상태가 아니다.

핵심 의미·구조 재현 사례의 해소 및 정상 개선 회귀 부재, 보정 오적용 부재, 실제 근거 위치 일치가 확인되지 않으면 배포를 보류한다. 전체 회귀·import graph·프런트 빌드·UI 점검·비공개 자료 제외·깨끗한 Git 상태·activeJobs 0 이후 백엔드→프런트 순서로 배포한다.

Firestore `adminSettings/detectCalibration.floor`와 Render `DETECT_INDEPENDENT_STATISTICS_ENABLED`만 기존 값을 백업하고 선택 갱신한다. 다른 설정을 통째로 덮어쓰지 않는다. 이전 운영 코드·두 설정을 롤백 기준으로 기록한다.

배포가 완료되면 1·6·24·72시간에 신규 의미·구조 훼손, 최종 검증 미완료/실패율, 보정 일치·무변화·불일치 비율, 장르·분량별 점수 분포, 실제 호출 비용·시간을 확인한다. 점검표 작성은 자동 모니터 등록과 다르며 예약 도구가 없으면 자동 점검을 완료했다고 표현하지 않는다.
