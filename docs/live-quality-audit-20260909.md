# 운영 원문·결과 및 AI 감지 전수 감사 (2026-09-09)

## 범위와 데이터 보호

- 휴머나이징: 2026-09-03 01:19 UTC 이후 운영 완료 347쌍, 문장 정렬 5,514쌍
- AI 감지: 2026-09-03 02:41 UTC 이후 운영 결과 333건
- 휴머나이징 모드: 기본 300건, 다듬기 5건, 고급 42건
- 운영 원문, 결과 전문, UID는 저장소에 넣지 않았다. 이 문서에는 비식별 집계와 재현 가능한 결함 유형만 기록한다.
- 실제 OpenAI 재호출 없이 저장된 결과와 결정론 감사기를 재생했다.

## 휴머나이징 분석 결과

문장 정렬 결과는 실질 재작성 2,610건, 가벼운 수정 987건, 동일 656건, 분할 236건, 병합 37건이었다. 자동 정렬 신뢰도가 낮은 문장은 858건이며, 누락 후보 5건은 실제 의미 누락 확정값이 아니라 수동 검토 대상이다.

과거 결과에서 신규 문제 신호가 나온 문서는 8/347건이었다. 순차 연결어 과잉 5건, 인접 의미 반복 3건, 담화 연결어 과잉 1건이며 한 문서에 복수 신호가 있을 수 있다. 신규 완전 중복 문장은 없었다. 구조 서명 실패는 1건이었다.

원문 자체의 PDF·OCR·복사 잔재는 47건에서 확인됐다. 가장 잦은 유형은 문장 공백 손상 27건, 닫히지 않은 구분자 8건, 한 행에 붙은 제목 7건, 문장 끝 부호 누락 7건, 강제 줄바꿈 4건이었다. 이 손상이 모델 입력에 남으면 출력에서 조사만 문단 첫머리에 남거나, 짧은 서술어가 독립 문장으로 남거나, 같은 주장이 요약문과 설명문으로 연속 생성될 수 있었다.

## 근본 원인과 수정

### 1. 입력 구조 정상화

- PDF 페이지 경계가 어절과 조사 사이에 빈 행을 넣은 경우도 모델 호출 전에 복원한다.
- 강제 줄바꿈 뒤에 짧은 서술부가 남는 패턴을 복원한다.
- 코드, 표, 독립 인용, 실제 제목과 목록은 같은 규칙의 대상에서 제외한다.
- 여러 절에서 AI 활용을 성찰하는 학습 보고서가 지원서로 오인되지 않도록 문서 프로필 신호를 보강했다.

### 2. 최종 문단·중복 안전장치

- 원문에 없던 쉼표·세미콜론 뒤 단일 줄바꿈과 빈 문단을 최종 전달 직전에 다시 잇는다.
- 한 원문 주장이 짧은 요약과 긴 설명으로 연속 복제된 경우 짧은 사본만 제거한다.
- 강제개행의 짧은 서술부가 다른 완결 문장 뒤에 고아 문장으로 남은 경우만 제거한다.
- 제목, 표, 목록, 인용, 법률 조문 및 원문에 실제로 있던 문장 반복은 삭제하지 않는다.
- 신규 관측값 `finalLayoutMidSentenceParagraphRepairCount`를 엔진 메타와 이력 허용 목록에 추가했다.

### 3. AI 감지 점수·원인 정합성

- 독립 통계 신호만으로 점수를 올리는 경우 상한을 49점으로 제한했다.
- 50점 이상은 모델이 실제 위치와 유형을 특정한 근거가 함께 있을 때만 허용한다.
- 원인 커버리지 계산에 검증된 통계 근거를 같은 계약으로 전달해 점수 보정 뒤 원인 설명을 다시 정렬한다.
- 통계 신호가 새로운 문장별 원인을 꾸며 내지는 않도록 기존 폐쇄형 원인 체계를 유지한다.

333건 재생 결과 원인·점수 정합 상태는 321건에서 330건으로 늘었고, 부분 정합은 10건에서 3건으로 줄었다. 통계만으로 50점 이상이 된 4건은 모두 49점으로 제한됐다. 남은 3건 중 2건은 현재 구조화 계약이 없던 구버전 기록이고, 1건은 낮은 점수에서 반복 원인 강도가 상대적으로 큰 역방향 불일치다.

## 재생 검증

- 새 입력 정상화 후보: 47건
- 새 쉼표 뒤 줄바꿈 복원 후보: 1건
- 새 고아 서술어·단일 주장 복제 제거 후보: 1건(두 규칙 동시 적용)
- 문서 프로필 판정 변경: 7건
- 신규 규칙 적용 후 전체 자동 테스트: 1,537/1,537 통과
- 집중 구조·감지 회귀: 94/94 통과
- production 의존성 감사: 취약점 0건

## 해석과 잔여 위험

- 과거 결과의 연결어 과잉 5건과 인접 의미 반복 3건은 새 버전이 생성한 결과가 아니므로 실제 감소율은 배포 후 운영 표본으로 확인해야 한다.
- 문장 정렬의 누락 후보 5건은 자동 정렬 실패와 실제 누락이 섞일 수 있어 의미 손실 건수로 사용하지 않는다.
- 구버전 AI 감지 2건은 원인 계약이 없어 재계산 없이 완전 정합으로 만들 수 없다. 신규 결과에는 현재 계약을 적용한다.
- 이번 변경은 기술 차단을 늘리지 않는다. 안전한 구조 복원과 감지 설명 정합성만 강화한다.
- 배포 후 24시간과 72시간에 구조 서명 실패, 고아 문장, 동일 주장 반복, 감지 부분 정합 비율을 다시 확인한다.
