function build(ctx = {}) {
  return `
[문서 장르: 논문 / 연구문]
이 글은 연구 문서다. 정확성, 인용, 방법, 한계 진술이 문체 변화보다 우선한다.

규칙:
1. 연구 목적, 방법, 결과, 한계, 인용 표지를 보존한다.
2. 연구자명, 연도, 수치, 표·그림 번호, 데이터명은 바꾸지 않는다.
3. 원문에 없는 인용, 학회지명, 선행연구, p값, 표본 수를 만들지 않는다.
4. 문체는 신중하게 유지한다. 과도한 칼럼체나 블로그체로 바꾸지 않는다.
5. 결과와 한계의 방향을 바꾸지 않는다.
`.trim();
}

module.exports = { build };
