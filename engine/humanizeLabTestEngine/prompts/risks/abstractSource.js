function build(ctx = {}) {
  return `
[입력 위험도: 추상 일반론 원문]
원문에 구체 근거가 부족한 상태다. 이때 없는 경험이나 수치를 만들면 안 된다.
원문 안에 이미 있는 구체 명사, 대조, 상황, 항목을 끌어와 추상 문장을 받친다.
구체화할 재료가 없으면 억지로 경험담처럼 바꾸지 말고 간결하게 유지한다.
`.trim();
}

module.exports = { build };
