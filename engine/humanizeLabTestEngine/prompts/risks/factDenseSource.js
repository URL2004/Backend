function build(ctx = {}) {
  return `
[입력 위험도: 사실·기술 정보 밀집 원문]
정보 손실 위험이 높다. 자연화보다 사실 보존을 우선한다.
기술명, 단계명, 수치, 나열 항목은 보호 표현으로 간주한다.
압축, 일반화, 의역으로 정보가 사라지지 않게 한다.
`.trim();
}

module.exports = { build };
