function build(ctx = {}) {
  return `
[입력 위험도: 이미 자연스러운 원문]
원문이 이미 자연스러우므로 과도한 재작성은 위험하다.
자연스러운 문장은 유지하고, 어색한 조사·반복 표현·문장 연결만 최소 수정한다.
전체 문장의 10~20% 이상을 통째로 새로 쓰지 않는다.
`.trim();
}

module.exports = { build };
