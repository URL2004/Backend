function build(ctx = {}) {
  return [
    '[입력 위험도: 구조 보존 중요]',
    '제목, 번호, 로마자 목차, 글머리표, 문단 순서를 유지한다.',
    '소제목을 본문에 합치거나 섹션 간 내용을 이동하지 않는다.',
    '깨진 줄바꿈만 정리하고, 원문 구조를 새로 설계하지 않는다.',
    '한 문장짜리 문단을 기계적으로 늘리거나 줄이지 않는다.'
  ].join('\n');
}

module.exports = { build };
