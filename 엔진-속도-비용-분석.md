# 엔진 실행 속도 · API 비용 분석

## 1. 왜 로컬 테스트가 느린가 (20분)

- **렌더(Render) 아님.** 렌더는 프로덕션 호스팅이고, 로컬 테스트(`engine-test.js`)는 사용자 PC에서 도는 거라 무관.
- **엔진 로직 자체는 밀리초.** 청크 분할·FLOOR 가드·surfaceguard 전부 즉시.
- 느린 건 **오직 LLM 호출**, 그리고 그게 지금 **claudecode 테스트 백엔드**라서:
  - `claude -p`(Claude Code CLI 헤드리스) = **호출당 ~40~60초** (단순 API 호출이 아니라 Claude Code 에이전트를 통째로 띄움 → 무거움).
  - 청크를 **하나씩 순차** 실행 (각 청크가 앞 청크 출력을 문체 연속성 참고로 받음 → 병렬 불가).
  - 경영 에세이 24청크 × ~40초 + judge 2~3회 ≈ **15~23분**.

## 2. 프로덕션(API)은 훨씬 빠름

- `LLM_BACKEND=api` (Anthropic API 직접 호출): 호출당 **~3~10초**, 청크 **병렬 가능**.
- 같은 24청크 글 → **1~3분, 병렬이면 수십 초**.
- **즉 20분은 테스트 환경(구독 CLI)의 한계이지 제품 속도가 아님.**

## 3. API 비용 추산 (모델 = Claude Sonnet)

경영 에세이(6.4K자 / 24청크 / judge 포함) 1회 기준:

| 단계 | 입력 토큰 | 출력 토큰 |
|---|---|---|
| 청크 재작성 24개 | ~50K | ~9K |
| judge (원장+검증) | ~11K | ~1K |
| 일부 repair | ~10K | ~2K |
| **합계** | **~70K** | **~12K** |

Sonnet 단가 ≈ 입력 $3 / 출력 $15 (per 1M):
- 입력 70K × $3 ≈ $0.21
- 출력 12K × $15 ≈ $0.18
- **≈ $0.4 / run ≈ 약 500~600원**

### 글 길이별
| 글 | 비용/run |
|---|---|
| 경영 에세이 (6.4K자, 24청크) | ~500~700원 |
| 일반 과제 (2~3K자, ~8청크) | ~150~250원 |
| + anti-detect 패스 | +약 50% |

### ★ 프롬프트 캐싱 적용 시
청크 24개가 같은 system 프롬프트(~2K토큰)를 매번 전송 → Anthropic 프롬프트 캐싱을 붙이면 23회는 10% 값으로 읽힘 → **입력 비용 ~1/3** → 경영 에세이 **~200~300원/run**. (현재 미적용. 실서비스 필수.)

## 4. 정리 / 권고
- 테스트 1회 = **수백 원**, 속도는 **20분 → 1~3분**.
- 반복 측정이 많으면 **(1) API 백엔드 연결 + (2) 프롬프트 캐싱**이 시간·비용 모두 압도적으로 유리.
- 빠른 테스트 대안: 짧은 발췌(5~6문단)로 측정하면 3~5분.

*(추산값 — 글 길이·캐싱·repair 횟수에 따라 변동)*
