# 엔진 실행 속도·API 비용 분석

2026-07-25 운영 기준 문서다. 운영 휴머나이징 provider는 OpenAI 하나이며
`engine-gpt-prod`의 Responses API 경로만 사용한다. Claude CLI·Anthropic API·
`LLM_BACKEND`·구형 `engine-test.js` 경로는 모두 폐기됐다.

## 처리시간을 만드는 단계

처리시간은 다음 실행 단계의 합으로 결정된다.

1. 원문 전처리, 구조 동결, 문서 프로필·voice 프로필 판정
2. 일반 편집 청크의 wave 처리
3. 의미·수치·기관·인용·화자·구조 감사
4. 필요한 문서만 국소 수리 또는 장문 섹션 회복
5. 레이아웃 재조립과 최종 전달 정책 판정

일반 편집 청크는 `HUMANIZE_CHUNK_CONCURRENCY` 범위 1~3에서 안정 순서를
보장하는 worker pool로 처리한다. 섹션 회복은 일반 청크와 동시에 실행하지 않는다.
ETA는 단순 청크 수가 아니라 처리 wave, 의미 감사, 회복 가능성, 대기열을 반영한다.

## 재시도와 상위 모델 사용

- 429는 `Retry-After`를 우선하고 최대 3회 재시도한다.
- 5xx·네트워크 오류는 최대 2회, timeout은 최대 1회 재시도한다.
- 스키마 오류는 같은 모델에서 한 번만 다시 시도한다.
- refusal·transport 실패는 품질 부족으로 간주하지 않으며 상위 모델 승격 사유가 아니다.
- 상위 모델은 안전하게 생성된 후보가 품질·의미 감사를 통과하지 못한 경우에만 제한적으로 쓴다.

각 청크에는 절대 처리 마감시간이 있어 재시도와 승격이 무한히 이어지지 않는다.

## 비용 관측

고정 추산표 대신 실제 Responses API usage를 기준으로 기록한다. 입력·캐시 입력·
출력·reasoning 토큰과 웹 검색 사용량을 합산해 작업별 `estimatedUsd`를 만든다.
모델 단가가 바뀔 수 있으므로 특정 글자 수의 원화 비용을 코드나 문서에 고정하지 않는다.

운영 출시 기준은 기존 버전 대비 작업당 API 비용 증가 30% 이하, p95 처리시간
증가 25% 이하다. 비용이나 시간만 초과하면 동시성을 1로 되돌리고 안전 감사와
전달·과금 정책은 유지한다.

## 검증

```powershell
npm test
npm run eval
npm run check:production-imports
node tools/transform-limits-test.js
npm audit --omit=dev
```

실제 API 속도와 비용은 관리자 무차감 실호출 및 저장소 밖의 익명화 재생 자료로
검증한다. 로컬 카피킬러 테스트 API는 이 검증과 운영 배포 범위에 포함하지 않는다.
